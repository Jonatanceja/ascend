---
const logos = [
  { src: '/facebook.svg', alt: 'Facebook', href: 'https://www.facebook.com/AscendLanguage/' },
];8
---
<footer class="bg-slate-800 py-10 md:py-20 relative z-20 mt-10">
    <div class="max-w-6xl mx-auto px-5 space-y-5">
        <h4 class="">Suscríbete a nuestro boletín </h4>
        <div class="text-white text-lg">
            Recibe las últimas actualizaciones, promociones exclusivas y consejos directamente en tu correo. Mantente informado y un paso adelante. 
        </div>
        <form class="space-y-5">
            <div class="flex flex-col md:flex-row gap-5">
                <label for="nombre" class="hidden">Nombre:</label>
                <input type="text" id="nombre" name="nombre" placeholder="Nombre" class="bg-white rounded-md p-3">
                <label for="correo" class="hidden">Correo electrónico:</label>
                <input type="email" id="correo" placeholder="Correo electrónico" name="correo" class="bg-white rounded-md p-3">
            </div>
            <input type="submit" value="Enviar" class="inline-flex items-center px-6 py-3 rounded-xl font-medium transition-colors bg-slate-800 text-white hover:text-slate-800 hover:bg-white border border-white ">
        </form>
        <div class="flex gap-5 md:gap-20 jusify-center md:justify-end items-center">
            {logos.map(logo => (
            <a href={logo.href} target="_blank" rel="noopener noreferrer" target="_blank">
                <img src={logo.src} alt={logo.alt} class="h-10 w-10 bg-slate-700 hover:bg-slate-600 p-2 rounded-full transition-colors" />
            </a>
            ))}
        </div>
        <div class="text-slate-400 text-sm text-center pt-5 border-t border-slate-600">© 2025 Ascend Language. Todos los derechos reservados</div>
    </div>
</footer>